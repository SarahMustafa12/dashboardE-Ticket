@* @model IEnumerable<Cinema>
<div class="container mt-5">
    @foreach (var item in Model) {
        <div class="card text-white bg-dark mb-3" style="max-width: 18rem;">
            <div class="card-body">
                <h5 class="card-title">@item.Name</h5>
                <p class="card-text">@item.Description</p>
                <p class="card-text">@item.Address</p>
                <p class="card-text">@item.CinemaLogo</p>
                

            </div>
        </div>
    }
</div>
 *@ 
@model IEnumerable<Cinema>


<div class="container mt-5">
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-4 mb-4">
                <div class="card" style="width: 18rem;">
                    <img src="~/images/cinema/@item.CinemaLogo" class="card-img-top" alt="Cinema Logo">
                    <div class="card-body">
                        <h5 class="card-title">@item.Name</h5>
                        <p class="card-text">@item.Description</p>
                    </div>
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item"><strong>Address:</strong> @item.Address</li>
                    </ul>

                </div>
            </div>
        }
    </div>
</div>
<div class="card mt-4">
    <div class="card-body d-flex align-items-center">
        <img src=""
             alt="Actor Image"
             class="rounded-circle border me-2"
             width="40" height="40" />
        <span>This is some text within a card body.</span>
    </div>
</div>

@* <div class="container mt-5">
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-4 mb-4">
                <!-- Each row has 3 columns -->
                <div class="card text-white bg-dark h-100">
                    <div class="card-body">
                        <h5 class="card-title">@item.Name</h5>
                        <img src="~/movie-ticket.png" alt="Cinema Logo" class="img-fluid mb-3">

                        <p><span class="fw-bold">Description:</span> <span class="fw-normal">@item.Description</span></p>

                        <p><span class="fw-bold">Address:</span> <span class="fw-normal">@item.Address</span></p>
                    </div>
                </div>
            </div>
        }
    </div>
</div> *@
  




